# 🏗️ Nairobi Sculpt - System Architecture

## High-Level Architecture Diagram

```
┌──────────────────────────────────────────────────────────────────────┐
│                          NAIROBI SCULPT                              │
│                    Aesthetic Surgery Management                       │
└──────────────────────────────────────────────────────────────────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
    ┌───▼────┐           ┌────▼────┐            ┌───▼──────┐
    │ BROWSER│           │  API    │            │ DATABASE │
    │        │           │  SERVER │            │          │
    └───┬────┘           └────┬────┘            └───┬──────┘
        │                     │                     │
        │                     │                     │
    ┌───▼──────────────┐  ┌───▼─────────────┐  ┌───▼────────┐
    │   WEB FRONTEND   │  │  NEST.JS API    │  │ POSTGRESQL │
    │  React 19        │  │                 │  │            │
    │  TypeScript      │  │ Auth Routes     │  │ 8 Models   │
    │  Vite            │  │ Validation      │  │ Relations  │
    │  TailwindCSS     │  │ Services        │  │ Indexes    │
    │  Router          │  │ Prisma ORM      │  │            │
    │  Query           │  │ JWT Middleware  │  │ Encrypted  │
    │  Testing         │  │ RBAC            │  │ Audit Log  │
    │                  │  │ Logging         │  │            │
    └──────────────────┘  └─────────────────┘  └────────────┘
         │                        │
         │                        │
    ┌────▼────┐            ┌─────▼──────┐
    │TanStack │            │ Prisma ORM │
    │Query    │            │ TypeSafe   │
    │Cache    │            │ Migrations │
    └─────────┘            └────────────┘
```

## Frontend Architecture

```
┌─────────────────────────────────────┐
│       REACT APPLICATION             │
│     (apps/web)                      │
└─────────────────────────────────────┘
         │
    ┌────┴──────┬────────┬────────┐
    │            │        │        │
┌───▼──┐   ┌────▼─┐  ┌──▼──┐  ┌─▼────┐
│Pages │   │Ctx   │  │Cmpt │  │Router│
│      │   │Auth  │  │Nav  │  │React │
│-Login│   │State │  │-Prot│  │Router│
│-Dash │   │JWT   │  │-Form│  │      │
│-Role │   │Token │  │-Card│  │      │
└──────┘   └──────┘  └─────┘  └──────┘
    │                    │        │
    │   ┌────────────────┴────────┘
    │   │
┌───▼───▼────────────────────────┐
│    TailwindCSS Styling         │
│  Brand Colors + Responsive     │
└────────────────────────────────┘
         │
    ┌────▼─────────────────────┐
    │  @nairobi-sculpt/ui      │
    │  @nairobi-sculpt/config  │
    │  @nairobi-sculpt/utils   │
    └──────────────────────────┘
```

## Backend Architecture

```
┌──────────────────────────────┐
│    NEST.JS APPLICATION       │
│     (apps/api)               │
└──────────────────────────────┘
         │
    ┌────┴───────────────┬─────────────┐
    │                    │             │
┌───▼───┐          ┌────▼────┐   ┌───▼───┐
│Modules│          │Services │   │Ctrl   │
│Auth   │          │Auth     │   │Auth   │
│Prisma │          │Audit    │   │       │
└───┬───┘          └────┬────┘   └───┬───┘
    │                   │            │
    │   ┌───────────────┴────────────┤
    │   │                            │
┌───▼───┴────────────────────────┐  │
│     Middleware Stack           │  │
│  CORS │ JWT │ Validation │RBAC │  │
└───┬────────────────────────────┘  │
    │                               │
    └──────────┬────────────────────┘
               │
         ┌─────▼──────────┐
         │  Prisma ORM    │
         │  Query Builder │
         │  Migrations    │
         └─────┬──────────┘
               │
         ┌─────▼──────────┐
         │   PostgreSQL   │
         │   Database     │
         └────────────────┘
```

## Data Flow

```
USER INTERACTION
      │
      ▼
┌─────────────────┐
│  React Frontend │
│  - User Input   │
│  - Form Submit  │
└────────┬────────┘
         │
         │ TanStack Query
         │ (with JWT token)
         ▼
┌─────────────────┐
│   API Server    │
│  - Validate     │
│  - Authorize    │
│  - Business     │
└────────┬────────┘
         │
         │ Prisma
         ▼
┌─────────────────┐
│  PostgreSQL     │
│  - Store/Fetch  │
│  - Index/Query  │
│  - Relations    │
└────────┬────────┘
         │
         │ Response
         ▼
┌─────────────────┐
│  React State    │
│  - TanStack Q   │
│  - Update UI    │
└─────────────────┘
```

## Authentication Flow

```
NEW USER
  │
  ├─ POST /auth/signup
  │  ├─ Validate input
  │  ├─ Hash password (bcryptjs)
  │  ├─ Create user in DB
  │  └─ Generate JWT tokens
  │
  └─ Response: {user, token, refreshToken}
     │
     └─ Store token in localStorage
        │
        └─ Redirect to dashboard


EXISTING USER
  │
  ├─ POST /auth/login
  │  ├─ Find user by email
  │  ├─ Compare password hash
  │  ├─ Generate JWT tokens
  │  └─ Log in auditlog
  │
  └─ Response: {user, token, refreshToken}
     │
     └─ Store token in localStorage
        │
        └─ Redirect to role dashboard


API REQUESTS
  │
  ├─ Add token to Authorization header
  │  (Bearer <token>)
  │
  ├─ JWT Middleware verifies
  │  ├─ Token valid?
  │  ├─ Not expired?
  │  └─ Correct signature?
  │
  ├─ RoleGuard checks
  │  └─ User role allowed?
  │
  └─ Execute handler
     └─ Log audit


TOKEN REFRESH
  │
  ├─ POST /auth/refresh
  │  ├─ Verify refresh token
  │  ├─ Generate new tokens
  │  └─ Return new pair
  │
  └─ Update localStorage
     └─ Continue using app
```

## Database Relationships

```
┌─────────────────┐
│      USER       │
│  id (PK)        │
│  email (UK)     │
│  password       │
│  name           │
│  role (FK)      │
│  isActive       │
└────────┬────────┘
         │
    ┌────┼────┐
    │    │    │
┌───▼──┐│  │┌─▼──────┐
│DOCTOR││  ││PATIENT  │
│Profi ││  ││Profile  │
└──────┘│  │└─────────┘
        │  │
    ┌───┴──┴────────┐
    │                │
┌───▼──┐        ┌───▼───┐
│APPTM │        │PROCEDURE
└──────┘        └────────┘
    │
    └─────────┬──────────┐
              │          │
         ┌────▼───┐  ┌──▼──┐
         │MEDICAL │  │AUDIT│
         │RECORD  │  │LOGS │
         └────────┘  └─────┘

AUDIT ├── User
LOG   ├── Action
      ├── Resource
      ├── Timestamp
      └── IP Address

INVOICE ├── Patient
        ├── Amount
        ├── Status
        └── Dates
```

## CI/CD Pipeline

```
CODE PUSH
   │
   ▼
┌─────────────────┐
│  GitHub (main)  │
│  or (develop)   │
└────────┬────────┘
         │
    ┌────┴────────────────────┐
    │                         │
    ▼                         ▼
┌──────────────┐      ┌──────────────┐
│ LINT WORKFLOW│      │ TEST WORKFLOW│
│- ESLint      │      │- Jest        │
│- Type check  │      │- Integration │
│- Prettier    │      │- Coverage    │
└──────┬───────┘      └──────┬───────┘
       │                     │
       └──────────┬──────────┘
                  │
                  ▼
           ┌─────────────┐
           │BUILD        │
           │WORKFLOW     │
           │- Frontend   │
           │- Backend    │
           └──────┬──────┘
                  │
        ┌─────────┴──────────┐
        │                    │
    ┌───▼────┐        ┌─────▼──┐
    │DEVELOP │        │MAIN    │
    │BRANCH  │        │BRANCH  │
    │        │        │        │
    │Deploy  │        │Deploy  │
    │Staging │        │Prod    │
    └────────┘        └────────┘
        │                  │
    ┌───▼────┐        ┌────▼────┐
    │VERCEL  │        │VERCEL   │
    │Staging │        │Prod     │
    └────────┘        └────────┘
```

## Deployment Architecture

```
┌──────────────────────────────────────────────────┐
│              NAIROBI SCULPT PROD                 │
└──────────────────────────────────────────────────┘
              │                      │
         ┌────▼────┐            ┌────▼──────┐
         │ VERCEL  │            │ RAILWAY   │
         │ Frontend│            │ Backend   │
         └────┬────┘            └────┬──────┘
              │                      │
         ┌────▼────┐            ┌────▼──────┐
         │React    │            │NestJS     │
         │App      │            │API        │
         │CDN      │            │Server     │
         └────┬────┘            └────┬──────┘
              │                      │
              │      ┌───────────────┘
              │      │
              └──┬───┴──┐
                 │      │
            ┌────▼──┐  ┌▼─────┐
            │AWS S3 │  │AWS   │
            │       │  │RDS   │
            └───────┘  └──────┘
                       (PostgreSQL)
```

## User Role Hierarchy

```
┌────────────────────────────────────────┐
│            USER ROLES                  │
└────────────────────────────────────────┘
         │
    ┌────┼────┬───────┬──────────┐
    │    │    │       │          │
┌───▼──┐│  │┌▼──┐  ┌──▼──┐   ┌─▼──┐
│ADMIN ││  ││FD  │  │DOC   │   │PAT │
└──────┘│  │└────┘  └──────┘   └────┘
        │  │
    ┌───┴──┴────┐
    │            │
    ▼            ▼
FULL ACCESS  OPERATIONAL
- Users      - Schedule
- Analytics  - Check-in
- Settings   - Patients
- Logs       - Queries
- Billing

DOCTOR              PATIENT
- Schedule          - Appointments
- Patients          - Records
- Procedures        - Billing
- Records           - Profile
- Documentation
```

## Shared Packages Architecture

```
┌──────────────────────────────────────────────┐
│        SHARED PACKAGES                       │
└──────────────────────────────────────────────┘
         │
    ┌────┼───────┬────────────┐
    │    │       │            │
    ▼    ▼       ▼            ▼
┌────┐┌──────┐┌─────┐    ┌──────┐
│UI  ││Config││Utils│    │Types │
└────┘└──────┘└─────┘    └──────┘
 │      │       │
 │      │       │
 │      │       └─ Dates
 │      │        - Validation
 │      │        - Formatting
 │      │        - LocalStorage
 │      │        - API Helpers
 │      │
 │      └────── Colors
 │             - Routes
 │             - Enums
 │             - API URLs
 │
 └─────────── Button
             - Input
             - Modal
             - Card
```

---

## Key Statistics

| Component | Count | Status |
|-----------|-------|--------|
| Database Models | 8 | ✅ Complete |
| API Endpoints | 3 | ✅ Implemented |
| React Components | 8+ | ✅ Ready |
| User Roles | 4 | ✅ RBAC |
| Authentication Methods | 1 (JWT) | ✅ Secure |
| CI/CD Workflows | 5 | ✅ Active |
| Test Suites | 2 | ✅ Ready |
| Documentation Files | 9 | ✅ Complete |

---

*This architecture is scalable, maintainable, and production-ready.*
